import React from "react";
import ReactDOM from "react-dom";
import {{pascalCase name}}  from "./{{pascalCase name}}.jsx";
/* eslint react/jsx-filename-extension: 0 */

export default function render{{pascalCase name}}() {
  const {{camelCase name}}s = [...document.querySelectorAll(".js-{{kebabCase name}}-mount")];
  if ({{camelCase name}}s.length > 0) {
    {{camelCase name}}s.forEach(mount => {
      const props = mount.dataset;
      ReactDOM.render(<{{pascalCase name}} {...props}/>, mount);
    });
  } else {
    return false;
  }
}
